---
title: "Configuration"
description: "Environment variables and global flags."
weight: 20
---

kr36 aims to need almost no configuration. As you add settings,
document each one here with its default.

## Global flags

| Flag | Meaning |
|---|---|
| `--help` | Help for any command |
| `--version` | Print the version |

## Environment variables

None yet. Add them here as the tool grows, prefixed `kr36_` in upper
case (for example `kr36_TOKEN`).
